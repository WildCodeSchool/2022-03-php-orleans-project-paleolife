<form method="post" action="{{ path('app_objective_delete', {'id': objective.id}) }}" onsubmit="return confirm('Voulez-vous supprimer ce service ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ objective.id) }}">
    <button class="btn btn-danger fs-5">
    <i class = " bi bi-trash-fill"></i>
    Supprimer</button>
</form>
