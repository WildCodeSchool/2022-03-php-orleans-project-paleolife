{% extends 'layoutAdmin.html.twig' %}

{% block title %}Modifier les nutritions
{% endblock %}

{% block body %}
	<main class="container">
		<h1 class="text-center m-5">Modifier les nutritions de
			{{ nutrition_meal.client.user.name }}</h1>
		<a class="btn btn-outline-secondary fs-5" href="{{ path('app_client_edit', {'id': nutrition_meal.client.id}) }}">
			<i class="bi bi-box-arrow-left me-2 "></i>
			Retour
		</a>
		{{ include('nutrition_meal/_form.html.twig', {'button_label': '<i class="bi bi-pen-fill me-2"></i> Modifier'})|raw }}
	</main>
{% endblock %}
