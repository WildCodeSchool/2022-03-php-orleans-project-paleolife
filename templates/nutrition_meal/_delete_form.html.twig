<form method="post" action="{{ path('app_nutrition_meal_delete', {'id': nutrition_meal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nutrition_meal.id) }}">
    <button class="btn">Delete</button>
</form>
