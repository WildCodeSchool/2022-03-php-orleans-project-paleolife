{{ form_start(form) }}
<div class="fs-5">
	{{ form_widget(form) }}
</div>
<div class="d-flex justify-content-between">
	<button class="btn btn-primary fs-5">
		<i class="bi bi-plus-circle me-2"></i>
		{{ button_label|default('Ajouter') }}</button>
	<a class="btn btn-dark text-light fs-5" href="{{ path('app_exercise_index') }}">
		<i class="bi bi-box-arrow-left me-2"></i>
		Retour</a>
</div>
{{ form_end(form) }}
