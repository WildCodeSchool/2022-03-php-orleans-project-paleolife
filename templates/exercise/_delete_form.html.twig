<form method="post" action="{{ path('app_exercise_delete', {'id': exercise.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cet exercice');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ exercise.id) }}">
    <button class="btn btn-danger fs-5"><i class="bi bi-trash-fill me-1"></i>Supprimer</button>
</form>
