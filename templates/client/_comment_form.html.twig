<form method="post" action="{{ path('app_comment_add', {'session': session.id}) }}">
    <label for="comment" class="form-label" placeholder="Mon avis sur cette séance">
        Commentaire
    </label>
    <textarea name="comment" id="comment" rows="5" class="form-control">
        {{ session.comment }}
    </textarea>
    <input type="hidden" name="_token" value="{{ csrf_token('add' ~ session.id) }}" />
    <button class="btn btn-primary fs-5">
        <i class="bi bi-pen me-1"></i>
        Valider   
    </button>
</form>
