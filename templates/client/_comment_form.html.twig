<form method="post" action="{{ path('app_comment_add', {'session': session.id}) }}" class="mt-3">
	<label for="comment" class="form-label font-td" placeholder="Mon avis sur cette séance">
		Commentaire

	</label>
	<textarea name="comment" id="comment" rows="5" class="form-control">{{- session.comment -}}
	</textarea>
	<input type="hidden" name="_token" value="{{ csrf_token('add' ~ session.id) }}"/>
	<button class="btn btn-primary font-td m-3">
		<i class="bi bi-pen me-1"></i>
		Valider
	</button>
</form>
