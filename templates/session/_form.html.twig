{{ form_start(form) }}
<div class="d-flex justify-content-between">
	<div class="col-6">{{ form_row(form.name)}}</div>
	{{ form_row(form.number)}}
</div>
{{ form_row(form.exercises)}}
<div class="d-flex justify-content-between">
	<button class="btn btn-outline-primary text-black border-2">
		<i class="bi bi-plus-circle me-2"></i>Valider</button>
	<button class="btn btn-dark text-light fs-5" href="{{ path('app_client_edit', {'id': session.client.id}) }}">
		<i class="bi bi-box-arrow-left me-2 "></i>Retour</button>
</div>
{{ form_end(form) }}
