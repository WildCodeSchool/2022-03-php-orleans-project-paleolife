{% extends 'base.html.twig' %}

{% block title %}Admin séance
{% endblock %}

{% block body %}
	<main class="container mb-5">
		<h1 class="d-flex justify-content-center my-5">Séance</h1>
		<a href="{{ path('app_session_new') }}" class="btn btn-button bg-primary mb-3 text-dark fs-5">
			<i class="bi bi-plus-circle me-2"></i>Ajouter
		</a>
		<table class="table">
			<thead class="fs-4">
				<tr>
					<th>Nom</th>
					<th>Numéro</th>
					<th>Actions</th>
				</tr>
			</thead>
			<tbody class="fs-5">
				{% for session in sessions %}
					<tr>
						<td>{{ session.name }}</td>
						<td>{{ session.number }}</td>
						<td>
							<a href="{{ path('app_session_show', {'id': session.id}) }}">show</a>
							<a href="{{ path('app_session_edit', {'id': session.id}) }}">edit</a>
						</td>
					</tr>
				{% else %}
					<tr>
						<td colspan="4">no records found</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	</main>
{% endblock %}
