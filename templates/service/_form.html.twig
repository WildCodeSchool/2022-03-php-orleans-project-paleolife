{{ form_start(form) }}
{{ form_widget(form) }}
<div class = "d-flex justify-content-between">
	<button class="btn btn-outline-primary text-dark fw-bold fs-5">
		<i class="bi bi-plus-circle me-2"></i>
		{{ button_label|default('Ajouter') }}</button>
	<a class="btn btn-outline-secondary text-dark fw-bold fs-5" href="{{ path('app_service_index') }}">
		<i class="bi bi-box-arrow-left me-2"></i>
		Retour</a>
</div>
{{ form_end(form) }}