<nav class="navbar sticky-top navbar-expand-md navbar-dark bg-dark">
	<div class="container-fluid flex-md-column justify-content-center align-items-center">
		<div class="my-2 w-25 d-none d-md-block">
			<img src="{{ asset('build/images/Logo.png') }}" class="w-100" alt="Logo">
		</div>
		<div class="w-25 d-block d-md-none">
			<img src="{{ asset('build/images/Logofeuille.png') }}" class="w-50" alt="Logo">
		</div>
		<button class="navbar-toggler ms-auto me-2" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNavAltMarkup">
				<div class="navbar-nav justify-content-between align-items-center gap-2 my-3">
					<a class="nav-item nav-link px-md-4 fs-4" href="{{ path('app_home') }}">Accueil</a>
					<a class="nav-item nav-link px-md-4 fs-4" href="{{ path('app_contact') }}">Contact</a>
					{% if app.user %}
						<a class="nav-item nav-link px-md-4 fs-4" href="{{ path('app_logout') }}">
							Se déconnecter</a>
					{% else %}
						<a class="nav-item nav-link px-md-4 fs-4" href="{{ path('login') }}">
							Se connecter</a>
					{% endif %}
					<a class="nav-item nav-link px-md-4 fs-4" href="{{ path('app_register') }}">Inscription</a>
				</div>
			</div>
		</div>
	</div>
</nav>
