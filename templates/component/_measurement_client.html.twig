<table class="table table-hover mb-3 p-3">
	<thead>
		<tr class="font-th">
			<th>Nom</th>
			<th>Avant</th>
			<th>Après</th>
		</tr>
	</thead>
	<tbody>
		{% for measurementClient in client.measurementClients %}
			<tr class="font-td">
				<td class="column">{{ measurementClient.measurement.name }}</td>
				<td class="column">{{ measurementClient.valueBefore }}  {{ measurementClient.measurement.unity }}</td>
				<td class="column">{{ measurementClient.valueAfter }}  {{ measurementClient.measurement.unity }}</td>
			</tr>
		{% endfor %}
	</tbody>
</table>
